using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class GameManager : MonoBehaviour
{
    //public Transform hammer1;
    //public Transform hammer2;
    public bool isGameRunning;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //t = Mathf.PingPong(Time.time * rotFl, 1f);
        //if (StackCollectable.crashedObstacle == false)
        //{
        //if (hammer1 != null)
        //hammer1.rotation = Quaternion.Slerp(Quaternion.Euler(270, 90, 0), Quaternion.Euler(180, 90, 0), t);
        //hammer2.rotation = Quaternion.Slerp(Quaternion.Euler(270, -90, 0), Quaternion.Euler(180, -90, 0), t);
        //}

    }
}
